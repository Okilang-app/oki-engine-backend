# Engineering Handoff — 2026-08-15

**Status:** Stopped for the day. Resume tomorrow from Task 2 Rights Gate unit test RED phase.

## Completed Today

### Stage 0 Foundation (ALL COMPLETE)

| Task | Commit | Tests | Review |
|---|---|---|---|
| Task 1 — Application kernel (errors, UUIDv7, correlation, logging, base app) | `8f13d3c..a213eeb` | 18 passed | Clean |
| Task 2 — PostgreSQL 18 / Compose / Valkey / SeaweedFS / UoW / mixins | `2156d64..dbf11d6` | 16 passed | Clean after fix round |
| Task 3 — Workflow state machine, idempotency, outbox, Hatchet integration | `bb2241d..098780b` | 55 passed | Clean after 2 fix rounds |
| Task 4 — Keycloak OIDC, RBAC, creator scope, realm export | `a4ab655..f6f2c72` + fix `c6a60bf` | 24 → 28 passed | Approved after fix round |

### Stage 1 Rights and Ingestion

| Task | Commit | Tests | Review |
|---|---|---|---|
| Task 1 — Creator, channel, agreement, grants, consent, RightsEvaluation table | `6a483e6..68a1dea` | 9 passed | Not yet reviewed; agent complete |

## In Progress / Partially Done

### Stage 1 Task 2 — Rights Gate (SUBSTANTIALLY COMPLETE, 16/18 PASSING, 2 TEST FIXTURES REMAIN)

**Files created but NOT committed:**
- `src/oki/rights/gate.py` — RightsGate service with evaluate(), require(), RightsGuardEvaluator adapter for Stage 0 Hatchet
- `src/oki/rights/policy.py` — Pure policy evaluator (no I/O), checks all 12 SOW deny cases
- `src/oki/rights/revocation.py` — RevocationService, propagates RIGHTS_REVOKED with atomic outbox/audit
- `tests/unit/rights/test_gate.py` — Parametric deny matrix (12 cases), approval/token test, GuardEvaluator adapter test
- `tests/integration/rights/test_revocation.py` — Revocation propagation, idempotency, missing version handling

**Test results (current, not committed):**
```
uv run pytest tests/unit/rights/test_gate.py tests/integration/rights/test_revocation.py -q
16 passed, 2 failed
```

**Remaining 2 test fixture fixes (not logic defects):**

1. **Expired test — `creator_already_exists`** (line ~300 in test_gate.py)
   - The expired case in `rights_case_factory` calls `create_agreement()` which tries to create a creator, but a creator already exists for this organization from the `no_agreement` test (or a prior test).
   - Fix: Reuse the already-created creator instead of calling `create_agreement()` again for expired.
   - Or: The `rights_case_factory` should create the creator once at the top and reuse it across cases, OR the `create_agreement` helper should accept an existing creator_id and skip creator creation.

2. **Channel test — wrong denial code** (`creator_approval_missing` instead of `channel_not_authorized`)
   - The `channel` test case returns a `RightsRequest` without `creator_approved=True`. Since the grant has `CreatorApprovalPolicy.EVERY_PUBLICATION`, the policy hits `creator_approval_missing` before reaching the channel check.
   - Fix: Add `creator_approved=True` to the channel test case RightsRequest.

**All production code changes have been applied:**
- `asset_category="education"` added to all RightsRequest bases
- `publication_channel_id` nullified in gate.py `_evaluate_in_uow` for denied evaluations
- Guard evaluator test now creates a real creator and passes its ID
- Tenant fixtures in both test files now thoroughly delete dependent rows (creators, channels, agreements, grants, evaluations, transitions, outbox, jobs, projects) before organizations
- Random `external_channel_id` per test call to avoid cross-module DB contamination
- Redundant organization INSERT removed from Guard evaluator test

**Next step:** Apply the 2 remaining fixes above, confirm full 18 tests pass, commit.

**Files created but NOT committed:**
- `src/oki/rights/gate.py` — RightsGate service with evaluate(), require(), RightsGuardEvaluator adapter for Stage 0 Hatchet
- `src/oki/rights/policy.py` — Pure policy evaluator (no I/O), checks all 12 SOW deny cases
- `src/oki/rights/revocation.py` — RevocationService, propagates RIGHTS_REVOKED with atomic outbox/audit
- `tests/unit/rights/test_gate.py` — Parametric deny matrix (12 cases), approval/token test, GuardEvaluator adapter test
- `tests/integration/rights/test_revocation.py` — Revocation propagation, idempotency, missing version handling

**Test results (not committed):**
```
uv run pytest tests/unit/rights/test_gate.py tests/integration/rights/test_revocation.py -q
9 passed, 9 failed, 1 error
```

**Remaining test fixture fixes (not logic defects):**
1. **Expired test** — DB trigger blocks UPDATE on rights_agreement_versions. Fix: set expires_at before approval.
2. **Asset category** — 5 tests fail because RightsRequest lacks `asset_category='education'`. Fix: add to all test requests.
3. **Channel FK** — denied evaluation fails with random UUID channel_id. Fix: nullify invalid channel_id before persistence.
4. **Guard evaluator test** — duplicates organization INSERT. Fix: remove redundant INSERT.
5. **Revocation teardown** — RESTRICT FK blocks organization deletion. Fix: delete workflow/outbox/job rows before organizations.
6. **Idempotency cleanup** — channel unique constraint from failed teardown. Fix: thorough cleanup order.

**Next step:** Fix the 6 test fixture issues above (not production code), confirm full 19 tests pass, commit.

**Files created but NOT tested or committed:**
- `src/oki/rights/gate.py` — Gate evaluator service
- `src/oki/rights/policy.py` — Pure policy functions (no I/O)
- `src/oki/rights/revocation.py` — Revocation propagation
- `tests/unit/rights/test_gate.py` — Unit tests
- `tests/integration/rights/test_revocation.py` — Integration tests

**Next step:** Agent was writing tests when stopped. Next person must:
1. Read the created files for correctness and completeness
2. Finish any missing test coverage
3. Run focused test: `uv run pytest tests/unit/rights/test_gate.py tests/integration/rights/test_revocation.py -q`
4. Fix any failures
5. Self-review, append to `.superpowers/sdd/2026-08-14-oki-backend-stage-1-rights-ingestion/task-2-report.md`
6. Selectively commit

### What Task 2 Must Implement

- `RightsGate.evaluate(request, now)` → `RightsDecision` (approved/denied + reason code)
- `RightsGate.require(request, now)` → `ApprovedRights` or raise before any provider work
- Pure policy: checks all SOW grant dimensions without I/O
- Service layer: loads current immutable agreement version, persists decision, returns unforgeable token
- GuardEvaluator adapter: wire to Stage 0 `src/oki/jobs/hatchet.py` so both API and Hatchet workers use same evaluator
- Revocation: set active jobs to `RIGHTS_REVOKED`, cooperative cancel, invalidate publication readiness, emit outbox/audit
- Every denial creates zero provider-usage rows

## Not Started (MVP Critical Path)

### Stage 1 Task 3 — tusd Resumable Upload / Immutable Assets
- Needs Task 2 (RightsGate) to exist
- Files: ObjectStore/S3 protocol, tusd hooks, asset models/service, `0005_assets_uploads.py`
- No YouTube scraping. Creator master bytes only.
- Brief: `.superpowers/sdd/2026-08-14-oki-backend-stage-1-rights-ingestion/task-3-brief.md`

### Stage 1 Task 4 — Media Validation → SOURCE_VALIDATED
- Needs Task 3 (assets)
- Files: `src/oki/media/command.py ffprobe.py ffmpeg.py`, validation service, Hatchet task, `0006_asset_validation.py`
- Brief: `.superpowers/sdd/2026-08-14-oki-backend-stage-1-rights-ingestion/task-4-brief.md`

### Stage 2 Partial — Sponsor Detection + Human Review
- Needs Task 4 (validated source)
- Files: `src/oki/sponsors/models.py detection.py service.py router.py`, `0008_sponsor_review.py`
- Brief: use Stage 2 plan Task 3 (sponsor review)

### Stage 3 Partial — Campaign Creative + FFmpeg Render
- Needs sponsor approved boundaries
- Files: `src/oki/campaigns/*`, `src/oki/sponsors/replacement.py`, `src/oki/renders/manifest.py ffmpeg_plan.py`, `0010_campaigns_creatives.py`, `0011_renders.py`
- Renumbered from original 0013/0014 because MVP skips dubbing/audio/shorts

## Running Migrations (current)

```
0001_foundation.py          — Stage 0 Task 2
0002_workflow_kernel.py     — Stage 0 Task 3 (includes dead_letters, checkpoints, provider_usage)
0003_identity_permissions.py — Stage 0 Task 4 (includes users, organizations, memberships, roles, permissions)
0004_creators_rights.py     — Stage 1 Task 1 (includes creators, agreements, grants, consents, RightsEvaluation)
```

**Migrations needed for MVP remainder:**
- `0005_assets_uploads.py`
- `0006_asset_validation.py`
- `0007_analysis_timeline.py` (minimal sponsor-only)
- `0008_sponsor_review.py`
- `0010_campaigns_creatives.py` (was 0013)
- `0011_renders.py` (was 0014)

## Environment

- PostgreSQL 18 on `localhost:55432` (Docker Compose)
- Database: `oki` / user: `oki` / password: `oki`
- Valkey on `localhost:6379`
- SeaweedFS S3 on `localhost:8333`
- Keycloak on `localhost:8080`
- ClamAV on `localhost:3310`

## Commands to Resume

```bash
cd .worktrees/backend-implementation

# Start services
POSTGRES_PORT=55432 POSTGRES_PASSWORD=oki docker compose up -d --wait

# Reset database to known state
uv run alembic upgrade head

# Stage 0 verification (should all pass)
uv run pytest tests/unit tests/integration/test_database.py tests/integration/test_health_dependencies.py tests/integration/jobs tests/integration/identity -q

# Stage 1 Task 1 verification
uv run pytest tests/unit/rights/test_agreement_versions.py tests/integration/rights/test_agreement_api.py -q

# Stage 1 Task 2 verification (next step)
uv run pytest tests/unit/rights/test_gate.py tests/integration/rights/test_revocation.py -q
```

## Known Issues / Risks

1. **FFmpeg not on workstation** — `ffmpeg` and `ffprobe` commands not found on Windows host. Worker Dockerfile must add FFmpeg for Tasks 4 and render.
2. **Stage 0 `.superpowers/...` gitignored** — SDD reports and progress.md must be force-added (`git add -f`) to commit.
3. **TrSession metadata sharing** — Creator models need identity `users` table. Previous fix imported identity metadata into creator models. Verify this pattern is stable.
4. **No live Keycloak/Hatchet in focused tests** — identity and workflow verified with real crypto/SDK but no live control-plane dispatch.
5. **Migration renumbering required** — MVP skips dubbing/audio/shorts, so original 0013→0010 and 0014→0011. No production history yet so renumbering is safe.

## Documents

- SOW Design: `docs/superpowers/specs/2026-08-14-oki-localization-backend-design.md`
- MVP Plan: `docs/architecture/video-ad-insertion-mvp.md`
- Team Context: `docs/architecture/TEAM_CONTEXT.md`
- Open Source Strategy: `docs/architecture/open-source-integration-strategy.md`
- Task Reports: `.superpowers/sdd/*/task-*-report.md`
- This Handoff: `docs/architecture/HANDOFF_2026_08_15.md`
